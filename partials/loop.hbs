{{! Previous/next page links - only displayed on page 2+ }}
<div class="extra-pagination inner">
    {{pagination}}
</div>

{{! This is the post loop - each post will be output using this markup }}
{{#foreach posts}}
{{!-- <article class="{{post_class}}">
    <header class="post-header">
        <h2 class="post-title"><a href="{{url}}">{{{title}}}</a></h2>
    </header>
    <section class="post-excerpt">
        <p>{{excerpt words="26"}} <a class="read-more" href="{{url}}">&raquo;</a></p>
    </section>
    <footer class="post-meta">
        {{#if author.image}}<img class="author-thumb" src="{{author.image}}" alt="{{author.name}}" nopin="nopin" />{{/if}}
        {{author}}
        {{tags prefix=" on "}}
        <time class="post-date" datetime="{{date format='YYYY-MM-DD'}}">{{date format="DD MMMM YYYY"}}</time>
    </footer>
</article> --}}
<div class="mdl-card mdl-cell mdl-cell--12-col {{post_class}}">
  <div class="mdl-card__media mdl-color-text--grey-50" style="background-image:url('{{image}}')">
    <h3><a href="{{url}}">{{{title}}}</a></h3>
  </div>
  <div class="mdl-color-text--grey-600 mdl-card__supporting-text">{{excerpt words="52"}}</div>
  <div class="mdl-card__supporting-text meta mdl-color-text--grey-600">
    {{!-- <div class="minilogo"></div> --}}
    {{#if author.image}}<div class="minilogo"><img src="{{author.image}}" alt="{{author.name}}" nopin="nopin" style="width:100%;" /></div>{{/if}}
    <div>
      <strong>{{author.name}}</strong>
      <span>{{date format="DD MMMM YYYY"}}</span>
    </div>
  </div>
</div>
{{/foreach}}

{{! Previous/next page links - displayed on every page }}
{{pagination}}
